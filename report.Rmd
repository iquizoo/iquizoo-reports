---
title: "大脑学习能力测评"
subtitle: "集体报告"
date: "2017年12月8日"
output:
  word_document:
    toc: true
    reference_docx: docs/word-styles-reference-report.docx
toc-title: "目录"
---

```{r load packages and data, message=FALSE, warning=FALSE, include=FALSE, paged.print=FALSE}
# load packages
library(tidyverse)
library(extrafont)
# configuration settings
data_folder <- "里水镇"
ability_names <- c("反应力", "注意力", "记忆力", "自控力", "思维力")
meta_vars <- c("userId", "姓名", "性别", "学校", "年级", "班")
lesson_vars <- c("语文", "数学")
rnk_cutedges_ability <- qnorm(c(0, 0.3, 0.7, 0.9, 1)) * 15 + 100
# load data
user_data <- read_rds(file.path(data_folder, "user_data.rds")) %>%
  filter(!is.na(姓名)) %>% 
  gather("能力", "得分", 持续注意:自控力, na.rm = TRUE) %>%
  # score of 0 indicates missing data
  mutate(得分 = ifelse(得分 == 0, NA, 得分)) %>%
  # some users have more than one record
  group_by_at(vars(one_of(c(meta_vars, "能力")))) %>%
  mutate(rank = row_number(desc(得分))) %>% 
  ungroup() %>% 
  filter(rank == 1) %>%
  # remain only variables of interest
  select(one_of(c(meta_vars, lesson_vars, "能力", "得分"))) %>% 
  filter(能力 %in% ability_names) %>% 
  # get the A-D rank for each ability
  mutate(rank = cut(得分, rnk_cutedges_ability, LETTERS[4:1])) %>%
  # change order of grade and class
  mutate(
    年级 = factor(
      年级, c("一", "二", "三", "四"), c("一年级", "二年级", "三年级", "四年级")
    ),
    班 = factor(班, 1:length(unique(班)))
  )
```

# 报告说明

---

“大脑学习能力”测评系统（以下简称学习能力测评）由北京师范大学脑与学习科学研究中心与成都爱智游科技有限公司联合编制。“大脑学习能力”测评系统整合了国际国内脑科学研究最新成果，依据脑功能分化和整合原理，以及大脑功能可塑性原理，采用计算机化认知测试的方式，全面测查与个体学习和发展密切相关的“大脑学习能力”。同传统智力测验相比，“大脑学习能力”测评具有科学性强、实施方便、准确度高、预测效度好、可反复测试等优势，代表了当今脑力测试的新趋势。

“大脑学习能力”测评综合考察注意力、记忆力、反应力、自控力等与人脑信息加工、学习、适应和发展密切相关的重要高级功能和大脑学习能力。这些能力覆盖了大脑的核心功能网络，是人脑智慧的集中体现。大量证据表明，个体在这些能力中的表现与其智力，学习能力，社会适应能力和未来发展潜力具有密切关系。

“大脑学习能力”测评提供多种能力维度指标，包含综合的大脑学习能力指数（Brain Learning Ablility Index, BLAI）和各个能力维度指数（注意力指数、记忆力指数、反应力指数、自控力指数）。

# 学生参加测评的情况

---

**测评年级**：1-4年级

**实测人数[^1]**：`r length(unique(user_data$userId))`人

```{r count users, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
user_data %>% 
  group_by(学校, 年级) %>% 
  summarise(人数 = n()) %>% 
  knitr::kable(caption = "各学校各年级实测人数[^count]")
```

**测评时间**：2016年12月

[^1]: 即实际测得数据中，至少包含五项能力中的一项的学生总数，可能会少于计划参加测评的学生总数。
[^count]: 学校排名不分先后。

# 第一部分 大脑学习能力指数

---

```{r calculate BLAI, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
BLAI <- user_data %>% 
  group_by_at(vars(one_of(c(meta_vars, lesson_vars)))) %>% 
  summarise(BLAI = mean(得分, na.rm = TRUE)) %>% 
  mutate(rank = cut(BLAI, rnk_cutedges_ability, LETTERS[4:1]))
```

综合能力分数由大脑学习能力指数（BLAI）表示，它反映了注意力、记忆力、自控力、反应力这四方面认知能力的综合水平。

```{r summary statistics for BLAI by grade, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
BLAI %>% 
  group_by(学校, 年级) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(BLAI),
    标准差 = sd(BLAI),
    最高分 = max(BLAI),
    最低分 = min(BLAI),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, caption = "BLAI指数简表[^2]", longtable = TRUE)
```

从上表可以看出，与全国平均水平（100分）相比，各年级BLAI指数得分略微偏低。从A，B，C，D四个等级人数分布看来，各年级BLAI指数有发展潜力的人数都比较多。如果对这些学生做进一步的针对性训练，预期会有很好的效果。

[^2]: A，B，C，D四个等级分别表示优秀（全国同龄水平90%以上），良好（全国同龄水平70%-90%），普通（全国同龄水平30%-70%），不足（全国同年龄水平30%以下）。表中列出了处于各个等级的学生人数。

```{r summary statistics for BLAI by class, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
BLAI %>% 
  group_by(学校, 年级, 班) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(BLAI),
    标准差 = sd(BLAI),
    最高分 = max(BLAI),
    最低分 = min(BLAI),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, caption = "各年级各班BLAI指数简表", longtable = TRUE)
```

从上表可以看出，学生的BLAI指数在相同年级的不同班级之间也存在较大差异。其中和顺中心小学一年级9班整体水平明显较差，存在较多BLAI指数偏低的学生。

## 各年级成绩与BLAI指数的相关关系

```{r Chinese and BLAI, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "语文"
ggplot(BLAI, aes_string("BLAI", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0("BLAI与", exam_name, "成绩的关系"),
    x = "BLAI指数",
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

```{r Math and BLAI, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "数学"
ggplot(BLAI, aes_string("BLAI", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0("BLAI与", exam_name, "成绩的关系"),
    x = "BLAI指数",
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

图中每一个点代表一个学生。从图中可以看出，学校之间无论BLAI指数还是学习成绩都没有较大差异，可以说9所学校的学生发展比较均衡。同时，根据图中对BLAI指数和学习成绩的拟合情况来看，BLAI指数能较好预测学生的学习成绩，并且随着年级的增长，预测效果越来越好。这表明随着年龄的增长，大脑基本认知能力将在学生的学习过程中发挥越来越重要的价值。

# 第二部分 注意力指数

---

```{r extract ATTENTION, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
ability_name <- "注意力"
ATTENTION <- user_data %>% 
  filter(能力 == ability_name)
```

注意是大脑接收和处理个体外部和内部信息的前提，只有被注意到的信息才能得到有效的分析和处理，并用来解决问题并形成持久的记忆。

```{r summary statistics for ATTENTION by grade, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
ATTENTION %>% 
  group_by(学校, 年级) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0(ability_name, "得分简表"))
```

从上表可以看出，大部分学校一二年级学生的`r ability_name`整体水平稍优于全国平均水平，而三四年级学生的`r ability_name`整体水平则稍显不足。

```{r summary statistics for ATTENTION by class, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
ATTENTION %>% 
  group_by(学校, 年级, 班) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0("各年级各班", ability_name, "得分简表"))
```

从上表可以看出，相同年级的学生的`r ability_name`表现在班级与班级之间也存在较大差异。其中和顺中心小学一年级9班整体水平明显较差，存在较多`r ability_name`得分偏低的学生。但是从测评人数角度看，也存在较多学生数据缺失情况。

## 各年级成绩与`r ability_name`的相关关系

```{r Chinese and ATTENTION, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "语文"
ggplot(ATTENTION, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

```{r Math and ATTENTION, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "数学"
ggplot(ATTENTION, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

图中每一个点代表一个学生。从图中可以看出，学校之间无论`r ability_name`得分还是学习成绩都没有较大差异，可以说9所学校的学生发展比较均衡。同时，根据图中对`r ability_name`得分和学习成绩的拟合情况来看，虽然比不上BLAI指数的预测能力，`r ability_name`得分也能较好预测学生的学习成绩，并且随着年级的增长，预测效果越来越好。这表明随着年龄的增长，`r ability_name`将在学生的学习过程中发挥越来越重要的价值。

# 第三部分 记忆力指数

---

```{r extract MEMORY, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
ability_name <- "记忆力"
MEMORY <- user_data %>% 
  filter(能力 == ability_name)
```

注意是大脑接收和处理个体外部和内部信息的前提，只有被注意到的信息才能得到有效的分析和处理，并用来解决问题并形成持久的记忆。

```{r summary statistics for MEMORY by grade, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
MEMORY %>% 
  group_by(学校, 年级) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0(ability_name, "得分简表"))
```

从上表可以看出，各年级`r ability_name`得分与全国平均水平（100分）相当。从A，B，C，D四个等级人数分布看来，各年级都存在一部分在`r ability_name`方面具有发展潜力的学生。

```{r summary statistics for MEMORY by class, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
MEMORY %>% 
  group_by(学校, 年级, 班) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0("各年级各班", ability_name, "得分简表"))
```

从上表可以看出，各个学校同一年级的不同班级学生之间的`r ability_name`整体表现差别不大，这说明学生们的`r ability_name`发展相对比较均衡。

## 各年级成绩与`r ability_name`的相关关系

```{r Chinese and MEMORY, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "语文"
ggplot(MEMORY, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

```{r Math and MEMORY, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "数学"
ggplot(MEMORY, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

图中每一个点代表一个学生。从图中可以看出，学校之间无论`r ability_name`得分还是学习成绩都没有较大差异，可以说9所学校的学生发展比较均衡。同时，根据图中对`r ability_name`得分和学习成绩的拟合情况来看，`r ability_name`得分也能较好预测学生的学习成绩，并且随着年级的增长，预测效果基本相当。这表明`r ability_name`在学生的学习过程中一直发挥着重要作用。

# 第四部分 反应力指数

---

```{r extract REACTION, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
ability_name <- "反应力"
REACTION <- user_data %>% 
  filter(能力 == ability_name)
```

注意是大脑接收和处理个体外部和内部信息的前提，只有被注意到的信息才能得到有效的分析和处理，并用来解决问题并形成持久的记忆。

```{r summary statistics for REACTION by grade, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
REACTION %>% 
  group_by(学校, 年级) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0(ability_name, "得分简表"))
```

从上表可以看出，各年级`r ability_name`得分相对差于全国平均水平（100分）。从A，B，C，D四个等级人数分布看来，各年级都存在较大比例在`r ability_name`方面具有发展潜力的学生。

```{r summary statistics for REACTION by class, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
REACTION %>% 
  group_by(学校, 年级, 班) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0("各年级各班", ability_name, "得分简表"))
```

## 各年级成绩与`r ability_name`的相关关系

```{r Chinese and REACTION, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "语文"
ggplot(REACTION, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

```{r Math and REACTION, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "数学"
ggplot(REACTION, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

图中每一个点代表一个学生。从图中可以看出，学校之间无论`r ability_name`得分还是学习成绩都没有较大差异，可以说9所学校的学生发展比较均衡。同时，根据图中对`r ability_name`得分和学习成绩的拟合情况来看，`r ability_name`得分也能较好预测学生的学习成绩，并且随着年级的增长，预测效果越来越好。这表明随着年龄的增长，`r ability_name`将在学生的学习过程中发挥越来越重要的价值。

# 第五部分 自控力指数

---

```{r extract CONTROL, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
ability_name <- "自控力"
CONTROL <- user_data %>% 
  filter(能力 == ability_name)
```

注意是大脑接收和处理个体外部和内部信息的前提，只有被注意到的信息才能得到有效的分析和处理，并用来解决问题并形成持久的记忆。

```{r summary statistics for CONTROL by grade, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
CONTROL %>% 
  group_by(学校, 年级) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0(ability_name, "得分简表"))
```

从上表可以看出，各年级`r ability_name`得分差异明显，一二年级表现与全国平均水平（100分）相当，而高年级相对稍微差于全国平均水平。从A，B，C，D四个等级人数分布看来，各年级都存在一部分在`r ability_name`方面具有发展潜力的学生。

```{r summary statistics for CONTROL by class, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
CONTROL %>% 
  group_by(学校, 年级, 班) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0("各年级各班", ability_name, "得分简表"))
```

从上表可以看出，相同年级的学生的`r ability_name`表现在班级与班级之间也存在较大差异。其中和顺中心小学一年级9班整体水平明显较差，存在较多`r ability_name`得分偏低的学生。

## 各年级成绩与`r ability_name`的相关关系

```{r Chinese and CONTROL, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "语文"
ggplot(CONTROL, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

```{r Math and CONTROL, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "数学"
ggplot(CONTROL, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

图中每一个点代表一个学生。从图中可以看出，学校之间无论`r ability_name`得分还是学习成绩都没有较大差异，可以说9所学校的学生发展比较均衡。同时，根据图中对`r ability_name`得分和学习成绩的拟合情况来看，虽然比不上BLAI指数的预测能力，`r ability_name`得分也能较好预测学生的学习成绩，并且随着年级的增长，预测效果越来越好。这表明随着年龄的增长，`r ability_name`将在学生的学习过程中发挥越来越重要的价值。

# 第六部分 思维力指数

---

```{r extract THOUGHT, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
ability_name <- "思维力"
THOUGHT <- user_data %>% 
  filter(能力 == ability_name)
```

注意是大脑接收和处理个体外部和内部信息的前提，只有被注意到的信息才能得到有效的分析和处理，并用来解决问题并形成持久的记忆。

```{r summary statistics for THOUGHT by grade, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
THOUGHT %>% 
  group_by(学校, 年级) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0(ability_name, "得分简表"))
```

从上表可以看出，各年级`r ability_name`得分与全国平均水平（100分）相当。从A，B，C，D四个等级人数分布看来，各年级都存在一部分在`r ability_name`方面具有发展潜力的学生。

```{r summary statistics for THOUGHT by class, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
THOUGHT %>% 
  group_by(学校, 年级, 班) %>% 
  summarise(
    人数 = n(),
    平均分 = mean(得分),
    标准差 = sd(得分),
    最高分 = max(得分),
    最低分 = min(得分),
    A = sum(rank == "A"),
    B = sum(rank == "B"),
    C = sum(rank == "C"),
    D = sum(rank == "D")
  ) %>%
  knitr::kable(digits = 2, 
               caption = paste0("各年级各班", ability_name, "得分简表"))
```

从上表可以看出，各个学校同一年级的不同班级学生之间的`r ability_name`整体表现差别不大，这说明学生们的`r ability_name`发展相对比较均衡。

## 各年级成绩与`r ability_name`的相关关系

```{r Chinese and THOUGHT, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "语文"
ggplot(THOUGHT, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

```{r Math and THOUGHT, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
exam_name <- "数学"
ggplot(THOUGHT, aes_string("得分", exam_name, group = 1, color = "学校")) +
  geom_point() +
  stat_smooth(method = "glm") +
  facet_wrap(~ 年级, scales = "free") +
  theme_minimal() +
  labs(
    title = paste0(ability_name, "与", exam_name, "成绩的关系"),
    x = paste0(ability_name, "得分"),
    y = paste0(exam_name, "成绩")
  ) +
  theme(
    title = element_text(family = "SimHei"),
    plot.title = element_text(hjust = 0.5)
  )
```

图中每一个点代表一个学生。从图中可以看出，学校之间无论`r ability_name`得分还是学习成绩都没有较大差异，可以说9所学校的学生发展比较均衡。同时，根据图中对`r ability_name`得分和学习成绩的拟合情况来看，虽然比不上BLAI指数的预测能力，`r ability_name`得分也能较好预测学生的学习成绩，并且随着年级的增长，预测效果越来越好。这表明随着年龄的增长，`r ability_name`将在学生的学习过程中发挥越来越重要的价值。

# 第七部分 发展建议

---

为了促进孩子大脑学习能力的全面发展，提高大脑学习能力整体水平，我们提供以下建议，以供参考 :

## 01 父母要以身作则

---

父母是孩子最重要的学习榜样，所以首先要以身作则，给孩子树立一个良好的榜样，做事情的时候要专注、仔细、耐心，控制自己的脾气，灵活处理突发事件。这样，小孩在无意识的模仿学习的过程中也能慢慢的形成这些品质。

## 02 鼓励和表扬

---

当孩子全神贯注做某件事情时，建议家长要不失时机的给予鼓励和肯定。因为鼓励和表扬是对小孩行为表现的肯定，会促使小孩下次再表现出这些行为。例如，在小孩完成作业之后，对小孩说“刚刚你写作业的时候表现很认真，看到你一心一意的完成作业，妈妈感觉很佩服你！”

## 03 尊重发展规律

---

了解孩子的认知发展特点，尊重和理解孩子，不苛求孩子。例如：一般7—10岁儿童注意力能持续20分钟左右，思维力比较单一；青春期的小孩比较容易冲动等等。因此，当小孩的表现没有达到预期时，家长不要急于责骂，建议结合认知能力发展规律寻找原因。

## 04 培养设立目标的好习惯

---

只有当孩子明白自己想要做什么、应该做什么的时候，才能规范自己的行为朝着目标前进，可以鼓励孩子认真设立目标；将目标分享给同学、老师、家长，让他们监督自己；将目标写下来贴在家里，时刻提醒等。

## 05 认知能力训练

---

使用针对性的认知能力训练。认知能力训练主要是依据儿童认知能力发展规律，结合个体的认知能力发展特点，制定科学性的认知能力训练任务和训练日程，针对性地提高儿童认知能力水平。例如，为了加强思维力，可以让孩子每天坚持完成一些思维力训练的任务，并及时给出反馈。

